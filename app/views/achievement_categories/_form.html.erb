<%= form_with(model: achievement_category, local: true) do |form| %>
  <% if achievement_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(achievement_category.errors.count, "error") %> prohibited this achievement_category from being saved:</h2>

      <ul>
      <% achievement_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
